---
import Layout from '~/layouts/Layout.astro';
import Hero from '~/components/Hero.astro';
import Showcase from '~/components/Showcase.astro';
---

<Layout>
  <main>
    <Hero />
    <Showcase />
    <section class="section section--showcase">
      <p class="section__description">
        À travers ce portfolio, laissez vous transporter dans un univers ponctué de douceur, légèreté et précision. Je
        vous propose une balade entre la photographie, le graphisme et la direction artistique. Concevoir, créer,
        colorier, dessiner, gommer, photographier, filmer... La création n’a pas de limite, l’imagination non plus.
      </p>
    </section>
  </main>
</Layout>

<style>
  .section {
    line-height: 1;
    position: static;
    top: 0;
    z-index: 4;
    width: 100%;
    min-height: 100vh;
    min-height: 100dvh;
    background: var(--color-bg);
  }

  .section--showcase {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    justify-content: center;
    padding: 8rem 5vw 30vh;
    cursor: default;
  }

  .section__description {
    border-top: 1px solid #000;
    padding: 4vh 8vw;
    margin-bottom: 4rem;
    font-weight: 300;
    line-height: 1.8;
  }

  @media screen and (min-width: 53em) {
    .section {
      font-size: 1.45rem;
    }
  }
</style>
